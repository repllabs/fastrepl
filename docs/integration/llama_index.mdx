---
title: LlamaIndex
---

[LlamaIndex](https://github.com/run-llama/llama_index) is a data framework for LLM applications to ingest, structure, and access private or domain-specific data.

```python
import fastrepl
from fastrepl.llama_index import FastREPLCallbackHandler

ds = fastrepl.Dataset.from_dict({})
fastrepl_callback_handler = FastREPLCallbackHandler(dataset=ds)
```

```python
from llama_index.callbacks import CallbackManager

callback_manager = CallbackManager([fastrepl_callback_handler])
```

```python
service_context = ServiceContext.from_defaults(callback_manager=callback_manager)
query_engine = index.as_query_engine(service_context=service_context)
```

Try `FastREPL X LlamaIndex` on [Colab](https://colab.research.google.com/github/repllabs/fastrepl/blob/main/examples/llama_index.ipynb)
